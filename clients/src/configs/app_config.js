let DOMAIN = 'http://localhost:3000'

export let AppConfig = {
    API_URL: `${DOMAIN}/api`
}

export let LOCALES = {
    ENGLISH: { id: 1, name: 'en', label: 'ENGLISH' },
    VIETNAM: { id: 2, name: 'vn', label: 'VIETNAMESE' },
}